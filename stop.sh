#!/bin/bash

# 工作流编排系统停止脚本

echo "=== 工作流编排系统停止脚本 ==="

# 停止后端服务
echo "1. 停止后端服务..."
pkill -f "./bin/web" && echo "✅ 后端服务已停止" || echo "⚠️  后端服务未运行"

## 停止前端服务 (如果在运行)
#echo "2. 停止前端服务..."
#pkill -f "vite" && echo "✅ 前端服务已停止" || echo "⚠️  前端服务未运行"
#
#echo "🔄 所有服务已停止"